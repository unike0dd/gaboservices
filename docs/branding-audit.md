# Branding Audit

This repository was scanned for disallowed legacy branding phrases.

## Scan method

- Ran a case-insensitive content scan over tracked and untracked files, including hidden files.
- Verified that no disallowed phrases were present.

## Command used

```bash
rg -n -i --hidden --no-ignore "<redacted-patterns>"
```

## Result

No matches found.
